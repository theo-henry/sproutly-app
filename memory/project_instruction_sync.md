# Instruction Sync

Before writing code or project files, Codex should read `CLAUDE.md` and the files in `memory/`.

If those files are not up to date with the user's current project instructions, update them before continuing with implementation work.
