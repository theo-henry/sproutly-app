# Secrets required by the request-meal-plan edge function.
# Set the real values via:
#   supabase secrets set GOOGLE_APPS_SCRIPT_EMAIL_URL=... GOOGLE_APPS_SCRIPT_SECRET=... --project-ref <ref>
# or in the Supabase dashboard under Project Settings > Edge Functions > Secrets.
GOOGLE_APPS_SCRIPT_EMAIL_URL=
GOOGLE_APPS_SCRIPT_SECRET=
